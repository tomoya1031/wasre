<%= form_with(model: [product, comment] ) do |f| %>
    <div class="comment-text  form-group">
        <%= f.text_area :comment, class: "comment", class: "form-control" %>
        <%= f.submit "送信", class: "btn btn-base-1 comment-sbmit"%>
    </div>
<% end %>
